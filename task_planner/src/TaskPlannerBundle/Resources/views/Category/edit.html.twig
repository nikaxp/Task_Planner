{% extends "::base.html.twig" %}

{% block title %}Edit Category{% endblock %}

{% block body %}
    <button class = "btn btn-xs "
            onclick = "window.location.href='{{ path('showCategory', {'id': category.id}) }}' "
            type="button"
    >Back to category</button>
    <h2>Edit Category</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}